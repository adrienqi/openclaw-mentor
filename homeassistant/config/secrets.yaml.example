# Copy to secrets.yaml and fill in (secrets.yaml is gitignored).
# mentor_webhook_secret must match HA_WEBHOOK_SECRET in .env (with "Bearer " prefix).

mentor_webhook_secret: "Bearer YOUR_HA_WEBHOOK_SECRET_FROM_ENV"
